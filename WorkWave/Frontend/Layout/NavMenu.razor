<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm px-4">
    <div class="container-fluid">
       
        <a class="navbar-brand fw-bold text-primary fs-3" href="">
            Work<span class="text-white">Wave</span>
        </a>

        
        <button class="navbar-toggler" type="button" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>

        
        <div class="@NavMenuCssClass navbar-collapse justify-content-between">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item px-2">
                    <NavLink class="nav-link text-light" href="" Match="NavLinkMatch.All">
                        <i class="bi bi-house-door me-1"></i> Home
                    </NavLink>
                </li>
                <li class="nav-item px-2">
                    <NavLink class="nav-link text-light" href="jobs">
                        <i class="bi bi-briefcase me-1"></i> Browse Jobs
                    </NavLink>
                </li>
                <li class="nav-item px-2">
                    <NavLink class="nav-link text-light" href="post-job">
                        <i class="bi bi-plus-circle me-1"></i> Post a Job
                    </NavLink>
                </li>
            </ul>

            
            <div class="d-flex align-items-center gap-2">
                <NavLink class="btn btn-outline-light px-3" href="login">Login</NavLink>
                <NavLink class="btn btn-primary px-3" href="register">Register</NavLink>
            </div>
        </div>
    </div>
</nav>

@code {
    private bool collapseNavMenu = true;
    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}